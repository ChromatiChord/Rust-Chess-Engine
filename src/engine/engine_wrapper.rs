use crate::config::{self, PieceActionTrigger, BoardState, AvailablePieceMoves};

pub fn evaulation_position(board_state: &BoardState, available_moves: Vec<AvailablePieceMoves>, depth: i16,  alpha: i16, beta: i16) {
    println!("ENGINE!");

    // ignoring alpha beta for now

    // check if available moves is empty:
        // if so, generate available moves
    
    // for move in avail

}