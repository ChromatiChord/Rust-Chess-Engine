use crate::config::{self, PieceActionTrigger, BoardState, AvailablePieceMoves};

pub fn evaulation_position(board_state: &BoardState, available_moves: Vec<AvailablePieceMoves>, alpha: i16, beta: i16) {
    println!("ENGINE!");
    // check if available moves is empty:
        // if so, generate available moves
    
    // for move in avail

}