use crate::config::{self, PieceActionTrigger, BoardState};

pub fn evaulation_position(board_state: &BoardState, available_moves: (Vec<(i8, i8)>, Vec<PieceActionTrigger>)) {
    println!("ENGINE!");
    // check if available moves is empty:
        // if so, generate available moves
    
    // for move in avail




}